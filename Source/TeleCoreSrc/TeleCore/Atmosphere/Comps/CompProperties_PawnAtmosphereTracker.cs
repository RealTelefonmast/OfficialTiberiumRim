using Verse;

namespace TeleCore.Atmosphere.Comps;

public class CompProperties_PawnAtmosphereTracker : CompProperties
{
    public CompProperties_PawnAtmosphereTracker()
    {
        compClass = typeof(Comp_PawnAtmosphereTracker);
    }
}