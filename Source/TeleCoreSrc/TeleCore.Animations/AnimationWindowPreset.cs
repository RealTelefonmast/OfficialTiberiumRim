using TeleCore.SDGUI;
using UnityEngine;

namespace TeleCore;

public class AnimationWindowPreset : UIElement
{
    public AnimationWindowPreset(Rect rect) : base(rect)
    {
        //Define Menu Area
        // menu = Menu.At(rect);
        
        //Add Menu Items
        //menu.Add()
    }
}