using TeleCore.Comps;

namespace TeleCore.Atmospheres.CompProperties;

public class CompProperties_PawnAtmosphereTracker : Verse.CompProperties
{
    public CompProperties_PawnAtmosphereTracker()
    {
        compClass = typeof(Comp_PawnAtmosphereTracker);
    }
}